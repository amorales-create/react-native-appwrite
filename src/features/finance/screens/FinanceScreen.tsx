import React from 'react';
import { View, Text } from 'react-native';

const FinanceScreen = () => {
    return (
        <View className="flex-1 items-center justify-center bg-light-bg dark:bg-dark-bg px-6">
            <Text className="text-lg text-gray-500">Finanzas (Próximamente)</Text>
        </View>
    );
};

export default FinanceScreen;
